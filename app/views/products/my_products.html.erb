<div class="row">
  <% @products.each do |product| %>
    <div class="col-sm-6 col-md-4">
      <div class="thumbnail">
       <% if product.pictures.first %>
        <%= link_to (image_tag (product.pictures.first.image.url(:medium)), class: 'image'), product_path(product) %>
        <% else %>
          NA
        <% end %>
      </div>
    </div>
  <% end %>
</div>