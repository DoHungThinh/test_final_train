<div class="row">
<% @category.products.each do |p| %>
<div class="col-sm-6 col-md-3">
<div class="thumbnail">
<%= link_to image_tag(p.pictures.first.image.url(:medium)), product_path(p) %>
</div>
</div>
<% end %>
</div>